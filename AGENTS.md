# Suanhai Repo Template | AGENTS | 模板源仓协作入口规则

Suanhai Repo Template 是模板源仓。进入本仓后，默认按默认骨架来源仓处理，不按普通项目仓处理。

## 一、仓库判断

- 本仓中的改动可能被复制到后续新仓库，不应按局部单仓改动理解其影响范围。
- 修改前先判断当前对象属于：模板骨架、示例内容，还是占位内容。
- 不把模板中的默认对象自动当作团队级或工作区级制度。

## 二、入口规则

- 涉及模板中的 Markdown 正文时，先读对应 Markdown 写作契约。
- 涉及规范性模板、契约模板或其它高约束模板对象时，先判断其是否受规范性文档最小格式契约约束。
- 本文件只定义模板源仓入口规则，不替代上位 Charter、Contract 或独立设计文档。

## 三、保守修改规则

- 修改应聚焦当前模板对象，不顺手扩张到无关模板文件或无关骨架目录。
- 未经明确要求，不把单个项目的局部偏好直接提升为模板默认项。
- 变更模板骨架前，应先明确该改动会影响哪些未来仓库对象。

## 四、写法边界

- 不把示例内容写成自动制度。
- 不把占位内容写成正式规则。
- 不把建议写成既成事实，不把条件性判断写成无条件结论。
- 路径、文件名、目录名、命令、字段名与配置项使用反引号；对象本体按正式名称书写。
